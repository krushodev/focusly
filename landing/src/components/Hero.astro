<section class="relative min-h-screen flex flex-col justify-center items-center text-center px-6 pt-20 overflow-hidden">
  <div class="absolute top-[-20%] left-1/2 -translate-x-1/2 w-[600px] h-[600px] bg-violet-600/20 rounded-full blur-[120px] -z-10 animate-pulse-slow"></div>

  <div class="gsap-hero opacity-0 translate-y-10 space-y-6 max-w-4xl mx-auto z-10">
    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-white/10 bg-white/5 backdrop-blur-md">
      <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
      <span class="text-xs font-medium text-gray-300 tracking-wide uppercase">v1.0 Now Available</span>
    </div>

    <h1 class="text-6xl md:text-8xl font-display font-bold leading-tight tracking-tight">
      Minimal focus. <br />
      <span class="text-transparent bg-clip-text bg-gradient-to-r from-violet-400 to-cyan-300">Maximum flow.</span>
    </h1>

    <p class="text-xl text-gray-400 max-w-2xl mx-auto leading-relaxed">Reclaim your attention span. A beautifully designed Pomodoro timer built for deep work sessions, sans the distractions.</p>

    <div class="flex flex-col sm:flex-row gap-4 justify-center pt-8">
      <button class="px-8 py-4 bg-white text-black font-semibold rounded-xl hover:bg-gray-200 transition-all transform hover:scale-105 shadow-[0_0_40px_-10px_rgba(255,255,255,0.3)]">
        Download for macOS
      </button>
      <button class="px-8 py-4 bg-white/5 border border-white/10 text-white font-medium rounded-xl hover:bg-white/10 transition-all backdrop-blur-sm"> Open Web App </button>
    </div>

    <div class="pt-4 text-sm text-gray-500 flex justify-center gap-6">
      <span>macOS Universal</span>
      <span>•</span>
      <span>Windows 11</span>
      <span>•</span>
      <span>Linux</span>
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';

  gsap.to('.gsap-hero', {
    opacity: 1,
    y: 0,
    duration: 1.2,
    ease: 'power3.out',
    delay: 0.2
  });
</script>
